# Configure npm to use modern --omit flag instead of deprecated --production
# This prevents the "npm warn config production Use --omit=dev instead" warning
# Do NOT set production config here - it triggers the warning even when set to false
omit=dev
